# 规划



范畴有以下属性 :

- 对象集
- Hom 集
- 恒等箭头
- 复合运算

```tex
\category[
	key1=val1 % 属性 , field , 类似
	key2=val2 % 属性 , 
]{
	content   % 名称
}
```

---

如何做到组合与继承 ?

比如一个群既是 Set 范畴里的对象 又是 Grp 范畴里的对象

这里 Set 是一个 class , Grp 也是一个 class

---

任何一个数学对象都有

- 字符串 : 打印方式

- 类型 : 比如箭头属于 $\hom(\ldots,\ldots)$ , 对象属于某某范畴 , 等等

  > 但是类型的运算要怎么实现 ? 这又是一个问题 。。。

---

运算符 : 支持柯里化

- APL 表示法 :

  $\begin{array}{l|l}
  Z \leftarrow(X\times Y) &
  z = f~(x,y) \\
  (Z \leftarrow Y)\leftarrow X &
  z = (f~x)~y
  \end{array}$

- 正波兰表示法 :

  $\begin{array}{c|l}
  (X\times Y) \to Z & 
  f~(x,y) = z \\
  X\to (Y\to Z) &
  (f~x)~y = z
  \end{array}$

- 逆波兰表示法 :

  $\begin{array}{c|r}
  (X\times Y) \to Z & 
  (x,y)~f = z \\
  X\to (Y\to Z) &
  y~x~f = z
  \end{array}$

```tex
\mydef{\opr}{#1}{#2}{
  #1 #2 f
}

\oprA %<x<y
\oprA <x%<y
\oprA <x<y
\oprA <<y%<x
```

---

